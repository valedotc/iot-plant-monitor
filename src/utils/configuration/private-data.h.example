#pragma once

/*!
 * \file private-data.h
 * \brief MQTT broker credentials.
 *
 * Copy this file to private-data.h and fill in your credentials.
 * private-data.h is listed in .gitignore and must never be committed.
 */

#define MQTT_BROKER "your-broker-id.s1.eu.hivemq.cloud"
#define MQTT_PORT 8883

#define MQTT_USER "your-username"
#define MQTT_PASSWORD "your-password"
